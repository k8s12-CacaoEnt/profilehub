package com.goorm.profileboxapiuser.comm;

import org.springframework.stereotype.Component;

@Component
public class Utils {
//    public static <T> Optional<T> makeOptional(T data, String replaceData){
////        if(typeof(data))
//        return Optional.ofNullable(data).orElse(replaceData);
//    }
}
